#include<bits/stdc++.h>
using namespace std;

void explainextra(){
    // sort(v.begin(),v.end());
   sort(a+2,a+4);
    sort(a,a+n);
    int n=n.size();
   pair<int,int>a[]={{1,2},{2,1},{4,1}};
  // sort(a,a+n,comp);
}
int main(){
    vector<int>a;
    explainextra();
    return 0;
}